package Algorithm.BFS;

import java.io.*;
import java.util.*;

public class p5569_출근경로 {

    // (x, y)
    // Count(x, y) = count(x - 2, y) + count(x, y - 2)
    // Count(0,0) =
    public static void main(String[] args) throws FileNotFoundException {
        System.setIn(new FileInputStream("Input.txt"));



    }
}
